{
  "name": "svea-bus",
  "private": false,
  "version": "1.0.4",
  "type": "module",
  "files": [
    "dist",
    "index.d.ts"
  ],
  "main": "./dist/svea-bus.umd.cjs",
  "module": "./dist/svea-bus.js",
  "types": "./dist/main.d.ts",
  "exports": {
    "types": "./dist/main.d.ts",
    "import": "./dist/svea-bus.js",
    "require": "./dist/svea-bus.umd.cjs"
  },
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build && tsc -P tsconfig.dts.json",
    "test": "vitest",
    "test:coverage": "vitest run --coverage",
    "test:bench": "vitest bench"
  },
  "devDependencies": {
    "@vitest/coverage-c8": "^0.30.0",
    "mitt": "^3.0.0",
    "typescript": "^4.9.4",
    "vite": "^4.0.4",
    "vitest": "^0.30.0"
  }
}
